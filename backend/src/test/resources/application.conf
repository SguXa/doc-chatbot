ktor {
    deployment {
        port = 8080
        host = "0.0.0.0"
    }
    application {
        modules = [ com.aos.chatbot.ApplicationKt.module ]
    }
}

app {
    mode = "full"
    database {
        path = ":memory:"
    }
    data {
        path = "./data"
    }
}
